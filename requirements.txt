# DeepLOB reproduction - core dependencies
# Pin minor versions loosely; Colab may ship newer torch.
torch>=2.1
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
# 使用 train.py --config 读取 yaml 时才需要
pyyaml>=6.0
# For Colab notebook convenience (optional locally):
# jupyterlab>=4.0

# 开发 / 测试 / 质量门禁（团队协同时建议安装）
# pip install -r requirements.txt -r requirements-dev.txt 或直接装下面这些
pytest>=8.0
ruff>=0.16.0
ty>=0.0.64

