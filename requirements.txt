# DeepLOB reproduction - core dependencies
# Pin minor versions loosely; Colab may ship newer torch.
torch>=2.1
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
# 使用 train.py --config 读取 yaml 时才需要
pyyaml>=6.0
# For Colab notebook convenience (optional locally):
# jupyterlab>=4.0
