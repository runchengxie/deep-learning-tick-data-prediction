# 本地冒烟训练配置。命令行参数可以覆盖这些值。
# 运行：deeplob-train --config configs/base.yaml

dataset: random
data_path: null
meta_path: null
protocol: setup2
setup1_cfs: [1, 2, 3, 4, 5, 6, 7, 8, 9]
k: 10
epochs: 3
batch_size: 32
lr: 0.01
eps: 1.0
patience: 20
seed: 0
val_frac: 0.2
resume: true
num_workers: 0
device: cpu
checkpoint_dir: ./checkpoints
checkpoint_name: deeplob
