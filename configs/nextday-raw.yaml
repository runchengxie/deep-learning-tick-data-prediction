# 当前移动硬盘数据的端到端 snapshot MVP。
# 原始盘只读，产物写入项目 data/（已被 gitignore 忽略）。

snapshot_root: /mnt/data/hdd6t/quant-data-lake/raw/cn_a_share_level2/snapshot
basic_root: /mnt/data/hdd6t/quant-data-lake/raw/cn_a_share_level2/basic
benchmark_path: /mnt/data/hdd6t/quant-data-lake/reference/cn_market_reference/csi_all_a_000985.CSI.parquet
output_dir: ./data/nextday-raw-200

start_date: "2021-01-01"
end_date: "2025-12-31"

# time_ms 相对 09:30；只读取 14:30 <= tick < 14:55。
scan_start_time_ms: 18000000
signal_time_ms: 19500000

chunks_per_sample: 2
chunk_size: 100
min_valid_events: 150
top_n: 400
min_history_days: 120
liquidity_lookback_days: 20
min_liquidity_observations: 15

lower_quantile: 0.2
upper_quantile: 0.8
min_cross_section: 100

samples_per_shard: 2048
storage_dtype: float16
price_scale_bps: 100.0
volume_log_scale: 16.0
normalized_clip: 32.0
