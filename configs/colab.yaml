# Colab GPU 训练配置。数据路径、元数据路径和检查点目录由
# scripts/run_colab.py 在运行时传入。

dataset: fi2010
data_path: null
meta_path: null
protocol: setup2
setup1_cfs: [1, 2, 3, 4, 5, 6, 7, 8, 9]
k: 10
epochs: 100
batch_size: 32
lr: 0.01
eps: 1.0
patience: 20
seed: 0
val_frac: 0.2
resume: true
num_workers: 2
device: cuda
checkpoint_dir: ./checkpoints
checkpoint_name: deeplob
